// break/continue

int main() {
    int x = 1;
    while (x < 5) {
        if (x == 2)
            continue;
        if (x == 4)
            break;
        printInt(x);
        x++;
    }
    return 0;
}
