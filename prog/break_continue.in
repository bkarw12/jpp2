// break/continue

int x;

int main() {
    int x = 1;
    
    while (x < 6) {
        if (x == 3)
            continue;
        if (x == 5)
            break;
        printInt(x);
        x++;
    }
    return 0;
}
