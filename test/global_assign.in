// redeklaracja funkcji

int main() {
    // int x = a(x);
    // x++;
    for (a = 1 to 5) {
        if (a >= 3) {
            if (a == 4) break;
        }
        printInt(a);
    }
    printInt(5);
    printString("\nHello world!");
    return 0;
}

int a = 5;
string b = "xD";
boolean c = 5 >= 2;
int x = 5 / 2;
